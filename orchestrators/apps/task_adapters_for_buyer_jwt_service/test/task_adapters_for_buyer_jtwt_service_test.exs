defmodule TaskAdaptersForBuyerJtwtServiceTest do
  use ExUnit.Case
  doctest TaskAdaptersForBuyerJtwtService

  test "greets the world" do
    assert TaskAdaptersForBuyerJtwtService.hello() == :world
  end
end
