defmodule TaskAdaptersForNotifyingServiceTest do
  use ExUnit.Case
  doctest TaskAdaptersForNotifyingService

  test "greets the world" do
    assert TaskAdaptersForNotifyingService.hello() == :world
  end
end
